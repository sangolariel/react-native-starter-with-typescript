import React from 'react';
import {View} from 'react-native';
import {ThemeView} from '~/theme/components';

const Home = () => {
  return (
    <ThemeView>
      <View />
    </ThemeView>
  );
};

export default Home;
